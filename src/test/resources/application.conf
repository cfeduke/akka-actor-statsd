akka {
    loglevel = INFO
    log-dead-letters-during-shutdown = on
    actor {
        debug {
            lifecycle = off
        }
    }
}

deploymentzone {
    akka-actor-statsd {
        hostname = "127.0.0.1"
        port = 32768
        namespace = ""
    }
}
